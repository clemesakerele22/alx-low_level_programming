#include "main.h"
/**
 * mul - muitiplies two integers
 * @a: the first character to be multiplied
 * @b: the second character to be multiplied
 *
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
